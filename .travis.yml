language: node_js
cache:
  directories:
    - ~/.npm
    - node_modules
notifications:
  email: false
node_js:
  - 'stable'
script:
  - npm run lint
  - npm run build
deploy:
  provider: pages
  skip_cleanup: true
  NPM_TOKEN: $npm_token
  GH_TOKEN: $github_token
  local_dir: public
  on:
    branch: master
after_success:
  npm run semantic-release
branches:
  except:
    - /^v\d+\.\d+\.\d+$/
